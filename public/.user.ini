; PHP extensions required by Laravel on this domain (cPanel / LiteSpeed)
; Upload to public/.user.ini — wait 5 minutes after upload for changes to apply.

extension=mbstring
extension=pdo
extension=pdo_mysql
extension=mysqli
extension=openssl
extension=tokenizer
extension=xml
extension=ctype
extension=json
extension=bcmath
extension=fileinfo
extension=curl

display_errors=Off
